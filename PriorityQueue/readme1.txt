Description: 
This is a java program that demonstrates a priority queue implementation using a heap instead of an array.

Instructions:
When the program runs the user can enter s to display the heap, i to insert an integer value into the heap, r to remove the largest value or the root from the heap, and e to exit the program.